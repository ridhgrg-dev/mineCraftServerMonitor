# Copy via the setup script, which generates local random development credentials.
# Do not use these placeholders as production secrets.
POSTGRES_PASSWORD=REPLACE_WITH_RANDOM_HEX
APP_DB_PASSWORD=REPLACE_WITH_DIFFERENT_RANDOM_HEX
MIGRATION_DB_PASSWORD=REPLACE_WITH_DIFFERENT_RANDOM_HEX
PUBLIC_DOMAIN=REPLACE_WITH_YOUR_DOMAIN
ACME_EMAIL=REPLACE_WITH_YOUR_EMAIL
# Production release image identifier, not a credential.
RELEASE_VERSION=0.1.0
